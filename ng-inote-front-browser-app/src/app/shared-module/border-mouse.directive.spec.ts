import { BorderMouseDirective } from './border-mouse.directive';

describe('BorderMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderMouseDirective();
    expect(directive).toBeTruthy();
  });
});
